import type { Component } from "solid-js";

const Invoices: Component = () => {
  return (
    <div class="flex flex-col items-center justify-center h-screen w-full">
      <h1 class="text-4xl font-bold text-red">template</h1>
    </div>
  );
};

export default Invoices;
